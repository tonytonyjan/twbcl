# Place all the behaviors and hooks related to the matching controller here.
# All this logic will automatically be available in application.js.
# You can use CoffeeScript in this file: http://jashkenas.github.com/coffee-script/

TWBCL.components = 
  readonly: () ->
    $('.component-os_objects-name-field input').attr("readonly", true)
    $('.component-os_objects-attributes-name-fields input').attr("readonly", true)
  new: () ->
    this.readonly()
  create: () ->
    this.readonly()
  edit: () ->
    this.readonly()
  update: () ->
    this.readonly()